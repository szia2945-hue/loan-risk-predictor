# Copy this file to .env and set a real, random secret key before deploying.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key

# Optional: point at a real database instead of the default local SQLite file
# DATABASE_URL=postgresql://user:password@localhost:5432/ledger
